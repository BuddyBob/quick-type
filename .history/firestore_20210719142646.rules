rules_version = '2';
service cloud.firestore {
  match /users/{itemId}/documents {
    match /{document=**} {
      allow read, write: if
          request.time < timestamp.date(2021, 8, 14);
    }
  }
}